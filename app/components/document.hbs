<div class="node document">
    <Input @type="checkbox" />
    <div class="icon document"></div>
    <div class="title">
        <LinkTo @route="authenticated.document" @model={{@model}}>
            {{@model.title}}
        </LinkTo>
    </div>
</div>