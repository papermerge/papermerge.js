<div class="node folder">
  <div class="icon folder"></div>
  <div class="title">
    <LinkTo
      @route="authenticated.nodes" @model={{@result_node.id}}>
      {{@result_node.title}}
    </LinkTo>
  </div>

  <Node::Tags
    @tags={{this.tags}}
    @more_than_max_visible_tags={{this.more_than_max_visible_tags}} />

</div>
