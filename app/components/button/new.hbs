{{#if @route}}
  <LinkTo
      @route={{@route}}
      class="btn btn-success" ...attributes>
        <i class="bi bi-plus-lg mx-1"></i>
        {{this.text}}
  </LinkTo>
{{else}}
  <button
    type="button"
    {{on "click" @onClick}}
    class="btn btn-success" ...attributes>
      <i class="bi bi-plus-lg mx-1"></i>
    {{this.text}}
  </button>
{{/if}}

