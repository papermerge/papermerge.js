{{#if @extranode}}
  <LinkTo
    @route="authenticated.nodes"
    @model={{@node.id}}
    @query={{hash extranode_id=@extranode.id}}>
      {{@node.title}}
  </LinkTo>
{{else}}
  <LinkTo
    @route="authenticated.nodes"
    @model={{@node.id}}>
      {{@node.title}}
  </LinkTo>
{{/if}}