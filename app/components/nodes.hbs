<label>
  <span>Search</span>
  <Input @value={{this.query}} class="light" />
</label>


<div class="nodes">
  <ul class="results grid">
      {{#each @nodes as |node|}}
        <li class="item"><Node @node={{node}} /></li>
      {{/each}}
  </ul>
</div>