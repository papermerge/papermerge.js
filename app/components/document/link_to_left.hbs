{{#if @extranode}}
  <LinkTo
    @route="authenticated.document"
    @model={{@node.id}}
    @query={{hash extranode_id=@extranode.id}}>
      {{@node.title}}
  </LinkTo>
{{else}}
  <LinkTo
    @route="authenticated.document"
    @model={{@node.id}}>
      {{@node.title}}
  </LinkTo>
{{/if}}